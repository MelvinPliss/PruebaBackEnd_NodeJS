version: '3.8'

services:
  mysql:
    image: mysql:9.6.0
    container_name: docker_mysql_alumnos
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: password
    ports:
      - "3307:3306"
    volumes:
      - ./db:/docker-entrypoint-initdb.d
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data:
